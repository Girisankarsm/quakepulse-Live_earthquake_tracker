[project]
name = "quakepulse"
version = "2.1.0"
description = "Global seismic intelligence platform powered by USGS live feeds"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
